# 2026-08-04

## 排查"mumuapi 公告"来源
- 现象：公告弹窗始终出现"mumuapi"公告，用户声称既非本地发布也非远端发布。
- 排查结论：整个代码库（backend + frontend）中不存在字符串 "mumuapi"。
- 后端逻辑（backend/app/api/announcements.py + services/announcement_client.py）：`WORKSHOP_MODE=server`（backend/.env）时公告**只读本地数据库** `backend/data/ai_story.db` 的 `announcements` 表；client 模式才从 `WORKSHOP_CLOUD_URL` 拉取云端。当前本地库 `announcements` 表为**空（0 条）**。
- 真正源头：前端 `frontend/src/hooks/useAnnouncements.ts` 会把公告缓存进浏览器 `localStorage`（键 `mobinovel_announcements_cached_items`，并从旧键 `mumu_announcements_cached_items` 一次性迁移）。弹窗组件 `AnnouncementModal.tsx` 会先渲染缓存再后台刷新。
- 因此 "mumuapi" 公告几乎确定是**历史遗留的浏览器 localStorage 缓存**（早期 client 模式从某云端同步过，或旧版本写入），并非本地库/远端当前发布。
- 解决办法：清空浏览器 localStorage 中的 `mobinovel_announcements_cached_items`（及旧键 `mumu_announcements_cached_items`、`mumu_announcements_read_ids` 等），刷新页面即可。

## 按用户要求简化公告逻辑（去掉本地/远端模式）
- 用户要求：公告不再区分"本地/server/client/远端"，直接始终读取本地数据库的公告，由管理员在系统设置里发布。
- 改动文件：
  1. `backend/app/api/announcements.py`：移除 `is_workshop_server()` 分支与 `announcement_client` 远端调用，公开接口（列表/同步）**始终读本地库**；`/status` 固定返回 `{"mode":"server","instance_id":"server"}`；`check_announcement_admin` 去掉"仅云端服务端"限制，本地管理员即可。
  2. `backend/app/services/announcement_client.py`：**已删除**（成为死代码）。
  3. `frontend/src/pages/SystemSettings.tsx`：`announcementAdminAvailable` 恒为 `true`；去掉"服务端/客户端"模式 Alert 与 Tag，改为"本地公告"；移除 `announcementStatus`/`loadAnnouncementStatus` 相关 state 与调用；更新相关文案。
  4. `frontend/src/hooks/useAnnouncements.ts`：当后端返回的 `active_ids` 为空数组时，清空本地缓存与已读 ID（防"幽灵公告"残留）。
- 注意：提示词工坊（`backend/app/api/prompt_workshop.py` + `services/workshop_client.py`）仍保留 server/client 云端模式逻辑，本次未改动（非公告功能）。
- 已提交并推送：commit `6e20bb3`（公告改本地逻辑）。

## 修复前端构建失败
- 现象：`npm run build` 退出码非零（tsc -b 报 TS6133）。
- 原因：`useAnnouncements.ts` 新增的 `active_ids` 为空清缓存逻辑里 `setReadIds(prev => {...})` 的 `prev` 参数未使用。
- 修复：改为直接 `saveReadIds([]); setReadIds([]);`，去掉未使用的 `prev`。
- 已提交并推送：commit `72c8b38`。构建已恢复（exit code 0）。

## 核查并修复"章节快照"功能 bug
- 用户部署到服务器（非本地），询问章节快照是否正常能用。
- 核查结论：
  - 迁移齐全：`20260803_1000_add_tianming_state_tracking.py`（建 items/secrets/vows/character_locations/chapter_snapshots 五表）+ `20260803_1100_add_character_current_location.py`（characters.current_location）。Docker 部署 entrypoint.sh 会自动 `alembic upgrade head`，服务器表结构 OK。
  - 闭环逻辑正确：`chapter_context_service.py` 用 `get_latest_snapshot` + `format_snapshot_for_context` 把快照注入下一章上下文。
- **发现并修复的真实 bug**（commit `1ede0c5`）：`snapshot_service.py` 的 `_build_15dim_snapshot` 中"地点状态/势力状态"维度错误引用 `Organization.is_organization == True` 和 `org.name`，但 `Organization` 模型（organizations 表）**没有**这两个字段（只有 character_id/power_level/member_count/location 等）。导致每次创建快照时该维度抛异常，快照始终建不出来（调用点虽 try/except 不影响章节生成，但天命闭环实际断了）。
  - 修复：改为从 `Character` 表过滤组织（`Character.is_organization==True`，组织名=`Character.name`），并 `outerjoin` 到 `Organization`（character_id 关联）取 power_level/member_count/location 详情。
- 备注：项目其他服务（auto_organization_service/validation_service/chapter_context_service）组织处理都正确，仅 snapshot_service 此 bug。
- 本地 `backend/data/ai_story.db` 是空库且缺 current_location 列（本地未跑迁移），非真实环境。
