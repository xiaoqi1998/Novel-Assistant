# 2026-08-06

## 章节分析新增"AI味分析"功能
用户需求：每个章节的分析中加入 AI 味（AI 写作痕迹）检测。已完整实现：

### 后端改动
- `backend/app/models/memory.py`：`PlotAnalysis` 新增 `ai_style_analysis` JSON 列（含 ai_score/level/overall/signs/strengths/suggestions），并输出到 `to_dict()`
- `backend/app/services/prompt_service.py`：`PLOT_ANALYSIS` 提示词新增"9. AI味分析"维度（判断句式工整/空洞形容词/情绪直描/对话书面化/过度总结等），输出 JSON 增加 `ai_style_analysis` 字段，约束部分新增 AI 味分析约束
- `backend/app/api/chapters.py`：保存分析结果时（更新+创建两条分支）持久化 `ai_style_analysis`
- `backend/app/services/plot_analyzer.py`：`generate_analysis_summary` 摘要文本加入 AI 味分析段落（概览页可见）
- `backend/app/schemas/import_export.py` + `import_export_service.py`：导入/导出剧情分析时同步 AI 味分析字段
- 新增 alembic 迁移：`backend/alembic/sqlite/versions/20260806_1000_add_ai_style_analysis.py`（sqlite）和 postgres 对应版本

### 前端改动
- `frontend/src/types/index.ts`：`AnalysisData` 增加 `ai_style_analysis?: AiStyleAnalysis | null`，新增 `AiStyleAnalysis` 接口
- `frontend/src/components/ChapterAnalysis.tsx`：新增"AI味分析" Tab（概览之后），展示 AI 味指数(0-100%)、等级 Tag、总体判断、典型AI痕迹（含原文引用）、人味亮点、去AI味建议；历史数据无该字段时提示重新分析

### 兼容性
- 旧分析记录无 `ai_style_analysis` 字段 → 前端显示"尚未进行AI味分析"提示，重新分析即可获取
- AI 未返回该字段时保存为 NULL，不影响既有分析流程
- 重启后端时 start.bat/update.bat 会自动执行 `alembic upgrade head` 完成迁移

### 备注
- 项目已有"去AI味"功能（story-deslop skill），写作时默认注入去AI味约束；本次是分析侧的 AI 味检测，两者互补
- 默认数据库 SQLite，生产可配 PostgreSQL，两个迁移目录均已添加对应文件

## 修复快照创建连续失败（2026-08-06 下午）
章节生成时快照多次 `⚠️ 快照创建失败，使用原始内容`，根因是 `snapshot_service.py` 的 `_build_15dim_snapshot` 把 Organization/CharacterRelationship 当 Character 用不存在的属性，逐个修复：
1. `Organization.is_organization` 不存在 → 去掉该过滤（organizations 表记录本身即组织）
2. `Organization.name` 不存在（组织名在关联 Character 上）→ 新增 `org_name_map`（character_id→name 映射），faction_states 用 `org_name_map.get(org.character_id, "未知组织")`
3. `CharacterRelationship.character_a_id/character_b_id` 不存在 → 改用 `character_from_id/character_to_id`
4. `prompt_service.PLOT_ANALYSIS` 模板第2180行 `{ai_score: 0-0.2, ...}` 未转义 → `str.format()` 把说明文字误当占位符报"缺少必需的参数: 'ai_score'"，改为 `{{ai_score: ...}}`
- 部署链路：本地 commit → push origin/main → 服务器 `bash auto-update.sh`（git pull + 构建 + 重启），本次部署后版本 12d7c4a
- 附带修复：`ChapterAnalysis.tsx` 两处 renderItem 未使用 `index` 参数触发 TS6133 构建失败，已删除
- 经验：模板里的 JSON 示例如 `{key: value}` 必须用 `{{key: value}}` 转义，否则 format 报缺参；可用正则 `\{[a-z_]+:\s` 扫描排查

## 远程清空项目章节内容
用户确认真实数据在远程（1Panel PostgreSQL）。通过 SSH 完成项目 `5777110b-4c6d-42af-9f97-e4d7d56548a5`（《千亿继承人的复仇：姑奶奶驾到》，one-to-many，24 章）全部章节内容清空：
- 24 章 content/summary 置 NULL、word_count=0、status='draft'；项目 current_words 归零
- 删除 plot_analysis 15 条、analysis_tasks 17 条、story_memories 322 条、分析来源伏笔 57 条（保留手动伏笔与大纲/标题）
- 容器内调用 `memory_service.delete_project_memories(user_id='newapi_1', project_id=...)` 清理向量库
- 关键坑：`docker exec` 必须加 `-i` 才能把 heredoc SQL 传给 psql，否则静默不执行
- 远程临时脚本已清理，详见 MEMORY.md 的部署环境记录

## 修复后台章节生成 NameError（2026-08-06 晚）
`POST /api/chapters/{id}/generate-background` 报 `NameError: name 'writing_skill_key' is not defined`（chapters.py:2281）：
- 根因：`generate_chapter_content_background` 构造 task_input 时引用了未定义的 `writing_skill_key`/`auxiliary_skill_keys`
- 修复：参照流式接口写法，在该函数内先定义 `_skill_key`/`_auxiliary_skill_keys`/`_writing_skill_key`，再写入 task_input；并补上后台执行函数 `_run_chapter_generation_bg` 的 task_input 传递（否则辅助 Skill 丢失）
- 教训：批量生成接口（batch）已有此传参约定，后台单章生成接口遗漏

## 修复 Claude 模型 temperature 400（2026-08-06 晚）
`claude-opus-4-8`（经 new-api 网关转发的 OpenAI 兼容端点）报 400：`temperature: This model does not accept a non-default temperature; omit it (the default is 1)`。
- 根因：用户/默认设置 temperature=0.7，OpenAI 客户端 `_build_payload` 无条件发送 temperature，Claude 系列模型只接受默认值 1
- 修复：`backend/app/services/ai_clients/openai_client.py` 的 `_build_payload` 中，模型名以 `claude` 开头时省略 temperature；temperature 为 None 或 ==1.0 时也省略（等价模型默认）
- 经验：Claude 新模型不再支持任意 temperature，只能省略或传 1；经网关转发同样受限

## 修复 claude-opus-4-8 的 messages system 位置错误（2026-08-06 晚）
new-api 网关转发 claude-opus-4-8 时上游返回 400：`messages.1: role 'system' must precede an 'assistant' message or end the array`
- 排查过程：应用层组装为 [system, user]（本地与服务器代码一致、服务器无 git 无法对比 commit）；确认错误为网关/上游对 system 消息的兼容校验；new-api 容器内无源码
- 修复：`backend/app/services/ai_clients/openai_client.py` 的 `_build_payload` 中，对 claude 系列模型把 system 消息内容合并到首条 user 消息（不再发送 role=system），彻底规避网关校验
- 附带发现（基础设施问题）：宿主机仅 3.9GB 内存且 swap 已用 2.6GB，new-api 网关持续报 `relay is unavailable because of cgroup memory pressure`(503) 自我保护拒绝转发；new-api 容器本身内存限制=0 仅用 27MB。需排查宿主机内存占用或扩容
- 经验：new-api 对 claude 模型的 system 消息存在兼容问题，应用层规避方式为合并到 user 消息；宿主机内存不足会导致网关 503 与请求超时（曾见 50s 耗时）

## 服务器存储排查与清理（2026-08-06 晚）
- 磁盘总 78G，清理前已用 63G（81%）、可用 16G
- 占用大头：Docker 镜像 20.86G、Build Cache 11.45G、new-api 源码 src/web 2.6G；应用自身数据很小（chroma_db 2M、novel-assistant 395M、PG 89M）
- 已执行安全清理：`docker builder prune -f` 释放 **7.82GB**；无悬空镜像（image prune 0B）
- 清理后：磁盘 55G/78G（**72%**），可用 **23G**
- 服务器同时跑着多个项目（xianyu 系列、lobechat、ai-comic-builder、allure-report 等），内存仅 3.9G 且 swap 2.6G，网关 memory_pressure 问题与磁盘无关，需单独处理

## 移动端适配全面升级（2026-08-06 晚）
用户需求"全都要"：深度优化 + 修复问题页面 + 新增移动端专属导航。9个任务全部完成：

### 已完成任务
1. **统一 isMobile 检测**：Outline/Settings/Login/ProjectList/AppFooter/short-story 全部改用 useIsMobile() hook；导出 MOBILE_BREAKPOINT(768) 和 TABLET_BREAKPOINT(1024) 常量
2. **Drawer 主题切换修复**：RootLayout + ProjectDetail 的 Drawer showCollapsedThemeButton=true；Drawer 宽度响应式（平板320px/手机280px）
3. **MobileBottomNav 组件**：新建 `frontend/src/components/MobileBottomNav.tsx`（大纲/角色/章节/更多底部导航），集成到 ProjectDetail
4. **项目统计移动端可见**：ProjectDetail 顶栏紧凑统计（大纲·角色·章·字），不再隐藏
5. **写作助手移动端可用**：Chapters.tsx 写作助手面板改为底部 Drawer(placement="bottom", height="65vh")，恢复移动端按钮显示
6. **编辑器配置区折叠**：Chapters.tsx 编辑器 Modal 配置区移动端用 Collapse(ghost, size="small") 包裹减少垂直空间
7. **Outline 场景描述展开**：Outline.tsx 新增 renderTruncate 辅助函数，场景字符串/location/characters/purpose/key_events/key_points 移动端点击 Popover 展开完整内容
8. **溢出修复**：Settings 预设列表操作按钮移动端改底部 Space wrap；Chapters 批量生成 Radio.Group 允许换行(flexWrap)；骨架屏按钮移动端左对齐+换行
9. **构建验证**：`npm run build` EXIT:0 编译通过；Playwright iPhone 13 视口截图验证登录页布局正常

### 关键文件修改清单
- `useIsMobile.ts` - 导出断点常量
- `Outline.tsx` - isMobile统一 + Popover展开 + 导入Popover
- `Settings.tsx` - 预设列表移动端操作换行
- `Chapters.tsx` - 写作助手Drawer + Collapse折叠 + Radio换行 + 骨架屏适配
- `ProjectDetail.tsx` - 底部导航集成 + 紧凑统计 + Drawer宽度
- `RootLayout.tsx` - Drawer主题切换恢复 + 响应式宽度
- `MobileBottomNav.tsx` - 新建组件
- `index.css` - 底部导航安全区域样式

### 技术方案要点
- 所有改动通过 isMobile 条件分支，桌面端行为零影响
- MobileBottomNav 使用 React.memo + Tabs(tabPosition="bottom")
- Popover 触发方式为 click（非 hover，适合触屏）
